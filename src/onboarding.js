class OnBoarding
{
    construct(currentStep, steps, isShutDown, baseDir, baseAdminDir)
    {
        this.currentStep = currentStep;
        this.steps = steps;
        this.isShutDown = isShutDown;
        this.baseDir = baseDir;
        this.baseAdminDir = baseAdminDir;
    }
}
